git remote add origin git@github.com:4xx2/demo.git	#将后面的链接重命名成origin
git branch -M 新分支	#分支改名
git push -u origin main	#将main分支推送到origin

git clone 链接	#克隆到本地
		#克隆到本地显示只有master
		#但实际上有所有分支，只是没有显示
git checkout 分支	#切换分支
		

git remote add origin 远程仓库地址	#给远程仓库地址起别名

git push origin 分支	#将代码提交到origin的该分支

git fetch origin dev	#将github的版本 拉成本地的版本
git merge origin dev	#将本地的版本 合并到工作区
